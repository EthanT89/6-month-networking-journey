# Phase 2 — Rasterization

**Goal:** Convert triangles into pixels. Build the z-buffer. Implement interpolation.

## Objectives

- [ ] SDL2 window setup — pixel buffer access
- [ ] Bresenham's line algorithm
- [ ] Bounding box triangle test
- [ ] Barycentric coordinates — inside/outside test
- [ ] Attribute interpolation across triangle surface
- [ ] Perspective-correct interpolation (w-divide)
- [ ] Z-buffer (depth buffer) — per-pixel depth test
- [ ] Back-face culling

## Checkpoint

Two overlapping triangles with correct depth ordering, regardless of draw order.

## Log

See `log/` for daily notes.
